#include "WorkThread.h"

// 클라이언트와 데이터 통신
DWORD WINAPI WorkThread(LPVOID arg)
{
	Reset_Ojbect();
	// send(오브젝트);

	while (true) {
		// Timer_Check();

		// 임계 영역 진입

		// EventQueue.exectueAll();

		// 임계 영역 탈출

		// 행렬 변환

		// 충돌 체크

		// 전송할 패킷리스트 개수 전송(고정크기)

		// for문으로 패킷 보내기
	}
	return 0;
}

void Reset_Ojbect()
{

}